$kind: http-request
url: "{{baseUrl}}/contoh/array-function"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 5000
