$kind: http-request
url: "{{baseUrl}}/buku/1/karakter"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 8000
