$kind: http-request
url: "{{baseUrl}}/buku/{{bukuId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 9000
