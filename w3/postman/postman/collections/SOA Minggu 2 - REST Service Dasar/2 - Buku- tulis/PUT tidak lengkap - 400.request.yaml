$kind: http-request
name: PUT tidak lengkap -> 400
description: PUT mengganti SELURUHNYA, jadi semua field wajib.
url: "{{baseUrl}}/buku/{{bukuId}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "judul": "Cuma Judul"
    }
scripts:
  - type: afterResponse
    code: pm.test('400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 6000
