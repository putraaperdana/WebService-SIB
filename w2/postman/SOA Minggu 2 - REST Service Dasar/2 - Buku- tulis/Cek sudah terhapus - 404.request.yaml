$kind: http-request
name: Cek sudah terhapus -> 404
url: "{{baseUrl}}/buku/{{bukuId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 10000
